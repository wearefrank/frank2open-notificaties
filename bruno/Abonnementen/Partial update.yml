info:
  name: Partial update
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{ROOT_URI}}/api/{{API_VERSION}}/abonnement/{{abonnement_uuid}}"
  body:
    type: json
    data: |-
      {
        "auth": "Bearer test-token-runner-patched"
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
